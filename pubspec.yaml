name: adbonnin_flutter_carousel
description: A carousel widget for Flutter with custom transition effects.
version: 1.2.0

homepage: https://github.com/adbonnin/flutter_carousel
repository: https://github.com/adbonnin/flutter_carousel

environment:
  sdk: ">=2.12.0-0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
